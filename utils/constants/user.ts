export enum UserRoleType {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum UserSortType {
  NAME_ASC = 'NAME_ASC',
  NAME_DESC = 'NAME_DESC',
}

export enum UserLoginType {
  CREDENTIALS = 'CREDENTIALS',
  GOOGLE = 'GOOGLE',
}
