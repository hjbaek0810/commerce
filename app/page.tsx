'use client';

import { useEffect, useRef, useState } from 'react';

import Banner from './sections/Banner';

const Home = () => {
  return (
    <main>
      <section>
        <Banner />
      </section>
    </main>
  );
};

export default Home;
